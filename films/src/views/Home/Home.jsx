import React from 'react';
import { Outlet } from 'react-router-dom';

const Home = () => {
    return (
        <div >
        
        home componjents
            <Outlet />
        </div>
    );
}

export default Home;
